---
  - hosts: localhost
    sudo: true
    serial: 1
    gather_facts: no
    roles:
      - ansible
      - vim
      - git
      - subversion
      - java
      - mysql-client
      - mongo-client
      - postgres-client
      - php-cli-env-dev
